<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#5B8DFF</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>

    </style>

    <style name="WhitePopUp" parent = "android:Theme.Dialog">
        <item name="android:windowBackground">@drawable/white_dialog</item>
    </style>

    <style name="LightGrayPopUp" parent = "android:Theme.Dialog">
        <item name="android:windowBackground">@drawable/light_gray_dialog</item>
    </style>

</resources>

